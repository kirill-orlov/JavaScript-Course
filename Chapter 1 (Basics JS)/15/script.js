'use strict'

let incr = 10,
    decr = 10;

    console.log(++incr);
console.log(--decr);

check = console.log( +'5' + +'10' );

console.log( '01' == 1 );

const isChecked = true,
      isClose = 0;

console.log(!!isClose);